name: Fly Deploy
on:
    push:
        branches:
            - main
            - release/*
    pull_request:
        branches:
            - main
            - release/*
env:
  FLY_API_TOKEN: $
  FLY_PROJECT_PATH: DotnetApi
jobs:
  deploy:
      name: Deploy app
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v2
        - uses: superfly/flyctl-actions@1.0
          with:
            args: "deploy"
